{
  "name": "flightline-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "tsx": "^4.23.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
